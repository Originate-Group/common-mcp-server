version: '3.8'

services:
  mcp-example:
    build:
      context: ../..
      dockerfile: examples/docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      # Add any environment variables your app needs
      LOG_LEVEL: "INFO"
    # For development: mount source code for hot-reload
    # volumes:
    #   - ../basic.py:/app/basic.py
    # command: uvicorn basic:app --host 0.0.0.0 --port 8000 --reload

networks:
  default:
    name: mcp-example-network
